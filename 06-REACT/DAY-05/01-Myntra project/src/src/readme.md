1.SRC->IN THTA I AM GOING TO HAVE   components,ulitls,main js file
2.components->components are used to make make ur code clean and better 
->separating all the js components differently
3.utils->all images ,videos,and audio and dummy text

4.import and export in js 
->when we have sepereated all the js component then it is not present in js main file 
so we need to call them,
we need to go to exact js component and there we just have to give command 

export default component name
eg->export default Header

where we calling it also we have to import it right

so for that u just need to give 

->import component-name and from "file path of importing component we it is present"
4.why react->coz it does all the all dom manipulation manually and it is easy 
